# Application
APP_ENV=development
APP_BASE_URL=
FRONTEND_BASE_URL=
API_BASE_URL=
COOKIE_DOMAIN=
CORS_ALLOWED_ORIGINS=

# Supabase — fill manually
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_STORAGE_BUCKET=document-private

# Database — fill manually if the worker uses a direct connection
DATABASE_URL=

# LLM provider credentials — intentionally blank for manual entry
GEMINI_API_KEY=
GEMINI_MODEL=
GEMINI_BASE_URL=

NVIDIA_API_KEY=
NVIDIA_BASE_URL=
NVIDIA_MODEL=

GROQ_API_KEY=
GROQ_BASE_URL=
GROQ_MODEL=

# Provider routing — set manually after keys/models are available
LLM_PROVIDER_PRIORITY=
LLM_REQUEST_TIMEOUT_SECONDS=30
LLM_MAX_ATTEMPTS=3

# Processing
MAX_FILE_SIZE_MB=20
MAX_FILES_PER_BATCH=20
JOB_LEASE_SECONDS=900
JOB_MAX_RETRIES=2
SIGNED_URL_TTL_SECONDS=300
RETENTION_DAYS=7

# n8n P1 only — intentionally blank
N8N_WEBHOOK_URL=
N8N_WEBHOOK_SECRET=

# Proxy/security
INTERNAL_WEBHOOK_SECRET=
RATE_LIMIT_PER_IP=
RATE_LIMIT_PER_USER=
